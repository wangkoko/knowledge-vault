# Summary: OpenCode Rules

## Source
`raw/rules_zh_tw.md`

## Context
This summary covers the rules for configuring custom instructions in OpenCode using `AGENTS.md`.

## Key Points
- Use `AGENTS.md` to provide custom instructions.
- Supports project-level and global-level rules.
- Compatible with Claude Code (`CLAUDE.md`).
- Can reference external files via `opencode.json` or `@` syntax.
